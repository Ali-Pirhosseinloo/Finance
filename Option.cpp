#include "Option.h"

Option::Option (const double& S, const double& K,const double& r, const double& v, const double& T): 
        S(S), 
        K(K),
        r(r),
        v(v),
        T(T)
        {}